name: grpc
version: 0.1.0

authors:
  - Jamie Gaskins <jgaskins@gmail.com>

crystal: 0.31.0

scripts:
  postinstall: mkdir -p bin; crystal build src/compiler.cr -o bin/grpc_crystal

executables:
  - grpc_crystal

targets:
  grpc_crystal:
    main: src/compiler.cr

dependencies:
  protobuf:
    github: jeromegn/protobuf.cr
    branch: master

license: MIT
