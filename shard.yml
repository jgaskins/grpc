name: grpc
version: 0.1.0

authors:
  - Jamie Gaskins <jgaskins@gmail.com>

crystal: 0.31.0

scripts:
  postinstall: pwd && ls -l && echo '...' && ls -l .. && shards build

executables:
  - grpc_crystal
  - protoc-gen-crystal

targets:
  grpc_crystal:
    main: src/compiler.cr

dependencies:
  protobuf:
    github: jeromegn/protobuf.cr
    branch: test-shards-install

license: MIT
